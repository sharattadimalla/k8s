
```
# check k8s version
kubectl version

# cluster info
kubectl cluster-info

# Get all k8s resources
kubectl get all

# Forward port to allow external access
kubectl port-forward <resource> <external_port>:<internal_port>

# create or modify a resource
kubectl apply -f deployment.yaml


```
